using NLog;
using NUnit.Framework;

namespace GameBot.Test.Misc
{
    [Ignore]
    [TestFixture]
    public class PaeduTests
    {
        private static readonly Logger _logger = LogManager.GetCurrentClassLogger();

        [Test]
        public void TestMe()
        {
        }
    }
}
